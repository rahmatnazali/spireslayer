from main import Deck, Card

defect_lightning_deck = Deck([
    Card(Card.LEAP),
    Card(Card.LEAP),
    Card(Card.LEAP),
    Card(Card.ZAP),
    Card(Card.ZAP),
    Card(Card.ZAP),
    Card(Card.ZAP),
    Card(Card.DEFRAGMENT),
    Card(Card.DEFRAGMENT),
    Card(Card.DEFRAGMENT),
    Card(Card.DEFRAGMENT),
    Card(Card.THUNDER_STRIKE),
])
